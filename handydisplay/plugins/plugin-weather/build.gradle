dependencies {
    implementation project(':handydisplay:core')

    javafx {
        javafx {
            version = JAVAFX_VERSION
            modules = [ 'javafx.controls', 'javafx.fxml' ]
        }
    }
}
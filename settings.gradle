rootProject.name = 'handy-display-kotlin'

include 'core'
project(':core').projectDir = file('subprojects/core')

include 'subprojects:widgets:weather'
findProject(':subprojects:widgets:weather')?.name = 'widget-weather'

include 'subprojects:widgets:api'
findProject(':subprojects:widgets:api')?.name = 'widget-api'

